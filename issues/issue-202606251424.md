# 課題解決プロンプト: デフォルトの `--thinking high` 指定を削除する

## 1. 役割定義 (Persona)
あなたは高度なソフトウェアエンジニアおよび問題解決のエキスパートです。以下のコンテキストとゴールを基に、正確かつ効率的な修正/実装を行ってください。

## 2. 背景・前提条件 (Context)
- **現状発生している問題**: `/workspace/pi-projects.ts` の `generatePiResumeFunc` が生成する pi 起動コマンドに、`--thinking high` フラグがハードコードされている。ユーザの要望により、このデフォルト指定を削除したい。
- **現状の該当箇所**:
  - `/workspace/pi-projects.ts:100` のコメント: `// 各 case では '--provider <p> --model <m> --thinking high --session <s>' の順で組み立てる。`
  - `/workspace/pi-projects.ts:129`: `flags` 配列内の `"--thinking high",` （プロジェクト別の通常ケース）
  - `/workspace/pi-projects.ts:147`: `'       pi --thinking high ;;',` （未知プロジェクトのフォールバックケース）
- **期待される挙動**: 上の2か所から `"--thinking high"` を削除し、生成されるシェルからも `--thinking` フラグが付与されないようにする。`pi` コマンド自体のデフォルト挙動に委ねる。

## 3. 解決すべきゴール (Goal & Objective)
- [ ] `/workspace/pi-projects.ts:129` の `flags` 配列から `"--thinking high",` の行を削除する
- [ ] `/workspace/pi-projects.ts:147` のフォールバックケースを `pi --thinking high ;;` から `pi ;;` に変更する
- [ ] `/workspace/pi-projects.ts:100` のコメントから `--thinking high` への言及を削除し、現在のフラグ順序（`--provider`, `--model`, `--api-key`, `--session`）に合わせた内容に更新する
- [ ] `generatePiResumeFunc` 関数の他の説明コメントで `--thinking high` を参照している箇所があれば併せて更新する
- [ ] `README.md` 等で `--thinking high` の挙動に言及している記述があれば修正する（存在しなければスキップ）
- [ ] 既存テスト (`*.test.ts`) をすべて pass させる。生成シェルのスナップショット/期待値テストに `--thinking high` を含むものがあれば更新する
- [ ] `npx oxlint` / `npx tsc` / `npx tsx --check` がすべてパスすることを確認する

### 制約事項
- `--thinking` 自体を残すかどうか（値なし/別デフォルト値で渡す等）の追加仕様変更は今回の対象外。完全削除のみを行う
- 既存の API シグネチャ（`generatePiResumeFunc`, `buildInitScript` 等）は変更しない
- 後方互換性のため設定ファイル (`pi-projects.json`) のスキーマは変更しない

## 4. 期待する成果物 (Output Format)
- 修正が完了した `/workspace/pi-projects.ts`（必要に応じて関連テスト・README）
- 修正理由とアプローチの簡潔な解説（PR 説明欄に流用できる粒度）
- テスト実行結果（`npm test` 等）と `npx oxlint` / `npx tsc` の実行結果
- 修正後の `generatePiResumeFunc` 出力シェル例（少なくとも1プロジェクト分のケース）

## 5. 実行開始の合図
このプロンプトを理解したら、「タスクを開始します」と宣言し、直ちにステップ2の「関連ファイル」の解析から始めてください。
