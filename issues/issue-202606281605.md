---
title: "pi-config.ts のパース系関数と getDefaultConfig の単体テスト追加"
status: DONE
created: 2026-06-28T16:05:00+09:00
---

# 課題解決プロンプト: pi-config.ts のパース系関数と getDefaultConfig の単体テスト追加

## 1. 役割定義 (Persona)
あなたは高度なソフトウェアエンジニアおよび問題解決のエキスパートです。以下のコンテキストとゴールを基に、正確かつ効率的な修正/実装を行ってください。

## 2. 背景・前提条件 (Context)
- **現状発生している問題**: `pi-config.ts` には13個の関数が定義されているが、`loadAiEnvConfig` 経由の間接的なテストしか行われていない。結果として以下が未検証：
  1. **エラーメッセージの品質**（ファイルパス、フィールド名、許可文字説明などが含まれているか）
  2. **境界ケース**（空文字列・空オブジェクト・配列など）
  3. **`getDefaultConfig`** 関数の出力（profiles に `pi-private`、projects に `pi-private` と randomUUID セッションを持つ設定が返ること）
  4. **`parseProfileOcrFields` / `parseProfileOptionalFields`** の必須/任意フィールドの判定ロジック
  5. **`parseProjectEntry`** の string/object 両対応分岐
  6. **`validateProjectKey`** のキー名バリデーション
  7. **`getAiEnvConfigPath`** の `AI_ENV_PI_PROJECTS` 環境変数による上書き
- **再現手順**: `pi-config.ts` の各関数とその単体テストの有無を確認する。
- **関連ファイル/コード**:
  - `pi-config.ts`: 13関数（`readConfigContent`, `parseConfigJson`, `toAiEnvConfigObject`, `toPlainObject`, `validateProjectKey`, `parseProfileOcrFields`, `parseProfileOptionalFields`, `parseProfileEntry`, `parseProfiles`, `parseProjectObjectValue`, `parseProjectEntry`, `parseProjects`, `getDefaultConfig`, `loadAiEnvConfig`）
  - `pi-projects.test.ts`: 現状は `loadAiEnvConfig` 経由のテストのみ

## 3. 解決すべきゴール (Goal & Objective)
- [ ] 各パース関数について「正常系」「型不正（string/object 以外）」「値不正（空文字・不正文字）」「必須欠落」の最低4パターンをテストする
- [ ] `getDefaultConfig` の出力構造（profiles / projects キー、`randomUUID` 形式のセッションID、`pi-private` プロファイル名）を検証する
- [ ] `getAiEnvConfigPath` の `AI_ENV_PI_PROJECTS` 環境変数による上書き動作を検証する
- [ ] `parseProjectEntry` の string 形式（後方互換）と object 形式の両分岐を検証する
- [ ] `toAiEnvConfigObject` の旧形式エラーメッセージ（`pi-projects.example.json を参考にしてください` 等の文言）を検証する
- [ ] 追加テストは **15件以上** を目標とする

## 4. 期待する成果物 (Output Format)
- 追加された `pi-config.test.ts`（テストファイル分割後）
- テスト実行結果（`npm test` で全件通過）

## 5. 実行開始の合図
このプロンプトを理解したら、「タスクを開始します」と宣言し、直ちにステップ2の「関連ファイル」の解析から始めてください。
