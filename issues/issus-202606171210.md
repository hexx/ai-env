現在開発中の CLI ツール `ai-env` について、プロジェクトごとに `pi` コマンドへ渡す API キーを柔軟に切り替えられるように機能を拡張してください。
以下の要件に従って、`pi-projects.ts` と `index.ts` を修正してください。

### 1. `pi-projects.ts` の修正（型の拡張とパース処理）
* `ProjectConfig` インターフェースに、オプションプロパティとして `apiKeyEnv?: string;` を追加してください。これは、APIキーとして参照するコンテナ内の環境変数名（例: `"LLM_API_KEY"`）を指定するためのものです。
* `parseProjectObjectValue` 関数を修正し、オブジェクトに `apiKeyEnv` が含まれている場合、`SAFE_SHELL_PATTERN` でバリデーションを行い、設定値として取り込むようにしてください。

### 2. `pi-projects.ts` の修正（コマンドの組み立て）
* `generatePiResumeFunc` 関数を修正し、プロジェクト設定に `apiKeyEnv` が存在する場合、`pi` コマンドのフラグに `--api-key "$<環境変数名>"` を追加するようにしてください。
* ※ コンテナ内のシェル実行時に環境変数として展開される必要があるため、Node.js 側での文字列組み立て時には、`--api-key "\\$${config.apiKeyEnv}"` のようにエスケープして `$` をそのまま出力するように気をつけてください。

### 3. `index.ts` の修正（クレデンシャルの追加）
* `CREDENTIAL_SOURCES` の配列に、仕事用のキーとして以下を追加してください。
  `{ args: ["find-generic-password", "-s", "LLM_API_KEY", "-w"], file: "security", name: "LLM_API_KEY" }`
* `buildEnvArgs` 関数を修正し、`--env=LLM_API_KEY=${params.credentials.LLM_API_KEY}` をコンテナに渡すようにしてください。
* （※前回の修正で追加した `--env=OPENCODE_API_KEY=${params.credentials.OPENCODE_API_KEY}` も忘れずに残してください）

これらの要件を満たす、完全な `index.ts` と `pi-projects.ts` のコードを出力してください。
