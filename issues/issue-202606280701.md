---
title: "テストコードの未使用変数回避策（ok(configPath.length > 0)）を改善する"
status: DONE
created: 2026-06-28T07:01:00+09:00
---

# 課題解決プロンプト: テストコードの未使用変数回避策（ok(configPath.length > 0)）を改善する

## 1. 役割定義 (Persona)
あなたは高度なソフトウェアエンジニアおよび問題解決のエキスパートです。以下のコンテキストとゴールを基に、正確かつ効率的な修正/実装を行ってください。

## 2. 背景・前提条件 (Context)
- **現状発生している問題**: `pi-projects.test.ts` 内で、`withTempConfig` のコールバックが受け取る `configPath` 引数が一部のテストで使用されていない。未使用変数の lint 警告を回避するために `ok(configPath.length > 0)` のような無意味なアサートが挿入されている。これはテストの意図を不明瞭にし、コードレビュー時に混乱を招く。
- **再現手順**: `pi-projects.test.ts` 内の以下の箇所を確認する：
  - L75付近: `ok(configPath.length > 0);`
  - L170付近: `ok(configPath.length > 0);`
  - L193付近: `ok(configPath.length > 0);`
- **関連ファイル/コード**:
  - `pi-projects.test.ts`: `withTempConfig` のコールバックシグネチャ、および全テストケース

## 3. 解決すべきゴール (Goal & Objective)
- [ ] 使用されない `configPath` 引数を `_configPath` にリネームし、`ok(...)` の偽アサートを削除する（TypeScript/ESLint は `_` プレフィックス付きの未使用変数を許容する）
- [ ] または `withTempConfig` のコールバックシグネチャから `configPath` を削除し、必要ないテストでは引数を取らないようにする
- [ ] 既存の全テストが引き続き通過すること

## 4. 期待する成果物 (Output Format)
- 修正された `pi-projects.test.ts` の差分
- テスト実行結果（`npm test` 全30件通過）

## 5. 実行開始の合図
このプロンプトを理解したら、「タスクを開始します」と宣言し、直ちにステップ2の「関連ファイル」の解析から始めてください。
