現在開発中の CLI ツール `ai-env` について、コンテナ起動時の初期化スクリプト内で実行している `socat` が落ちてしまう問題の修正をお願いします。

以下の要件に従って `pi-projects.ts` を修正してください。

### 修正要件
`pi-projects.ts` の `buildInitScript` 関数内で生成しているワンライナーの初期化スクリプトのうち、`socat` をバックグラウンド実行（`&`）している部分を、`pm2` を使ったプロセス管理に変更してください。

**現在の処理（変更前）:**
`socat UNIX-LISTEN:/home/pi/.config/herdr/herdr.sock,fork,reuseaddr TCP:host.docker.internal:9123 &`

**期待する処理（変更後イメージ）:**
`pm2 start socat --name "herdr-socat" -- UNIX-LISTEN:/home/pi/.config/herdr/herdr.sock,fork,reuseaddr TCP:host.docker.internal:9123`
※ `--` 以降に socat の引数が渡るように組み立ててください。プロセス名（`--name`）は分かりやすい任意の名前で構いません。

対象ファイル: `pi-projects.ts` `Dockerfile`
