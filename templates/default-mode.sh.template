#!/usr/bin/env bash
# デフォルト起動モード用初期化スクリプト
# 共通初期化 → プロジェクト名で case 解決 → herdr ブリッジ クリーンアップ

{{COMMON_SCRIPT}}

{{HERDR_PRESENCE_WAIT}}
project="$(basename "$PWD")"
case "$project" in
{{CASE_BODY}}
esac
rc=$?
cleanup_herdr_bridge
exit $rc